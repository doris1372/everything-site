# Everything ✨

Экспериментальный одностраничный сайт — "всё в одном месте". Калькуляторы, игры,
генераторы, рисовалка, часы, таймеры, пианино и ещё куча всего. Работает целиком
в браузере, без бэкенда.

## 🚀 Демо

После того как PR с GitHub Pages workflow смержится в `main`, сайт будет доступен
по адресу: **https://doris1372.github.io/everything-site/**

## 📦 Структура

```
everything-site/
├── index.html          # Точка входа
├── css/style.css       # Стили + темы (тёмная/светлая)
├── js/
│   ├── app.js              # Навигация, роутинг, поиск, тема
│   ├── sections.js         # Основные разделы (игры, утилиты, генераторы)
│   └── sections-extra.js   # +50 дополнительных разделов
└── .github/workflows/
    └── pages.yml       # Автодеплой на GitHub Pages
```

## 🧩 Разделы

Всего ~90 интерактивных разделов:

**Утилиты:** калькулятор, конвертер единиц, Base64, JSON форматтер,
текст-инструменты, Markdown превью, BMI, калькулятор возраста, секундомер,
таймер, Pomodoro, заметки, Todo, эмодзи, SHA-256, URL encode, regex тестер,
статистика текста, timestamp конвертер, CSV↔JSON, системы счисления,
чаевые, кредитный калькулятор, морзе, число прописью, проценты,
разница дат, unicode символы.

**Генераторы:** пароли, случайное число, Lorem ipsum, цвет + палитра, CSS
градиент, никнеймы, UUID v4, аватар по имени, штрих-код, мандала/спирограф,
CSS паттерны, фейковый юзер, генератор идей.

**Игры:** крестики-нолики (minimax), змейка, угадай число, RPS, память,
кликер, 2048, сапёр, поймай крота, тест печати, Саймон, понг, арканоид,
flappy, виселица, wordle, тест реакции, четыре в ряд, lights out,
пятнашки, матем. квиз, викторина по столицам.

**Развлечения:** кубик, монета, колесо фортуны, цитаты, анекдоты, рисовалка,
фейерверки, чат-бот, случайный факт, matrix rain, конфетти, калейдоскоп,
ASCII арт, трекер настроения.

**Звук:** пианино, метроном, генератор тона, драм-пад, синтезатор, белый шум.

**Инфо:** аналоговые + цифровые часы, календарь, бинарные часы, обратный
отсчёт до НГ, мировое время.

## 🛠 Локальный запуск

Никакой сборки не нужно — это чистый HTML/CSS/JS. Достаточно любого статического
сервера:

```bash
python3 -m http.server 8000
# или
npx serve .
```

Затем открыть `http://localhost:8000`.

## 🧑‍💻 Добавить новый раздел

Откройте `js/sections.js` и добавьте новый `reg({...})`:

```js
reg({
  id: "my-section",
  title: "Мой раздел",
  icon: "🌟",
  group: "Утилиты", // или Игры, Генераторы, Развлечения, Звук, Инфо
  desc: "Краткое описание",
  render(root, { toast, go }) {
    root.appendChild(/* ваш DOM */);
  },
});
```

Если раздел создаёт таймеры / слушателей `document`, очистите их через
`root._cleanup = () => { /* unsubscribe */ }` — роутер вызовет при переходе.

## 📄 Лицензия

MIT
